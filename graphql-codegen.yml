overwrite: true
schema: "./graphql/schema.graphql"
documents: null
generates:
  graphql/.generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
